// AI or Not? — Game Configuration
// All tunable parameters live here. Never hardcode these values in index.html.
const CONFIG = {
  APPS_SCRIPT_URL: "", // Empty = offline mode. Set to your Google Apps Script deployment URL.
  ITEMS_PER_SESSION: 10,
  CONTENT_URL: "content.json"
};
